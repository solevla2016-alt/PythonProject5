
Структура проекта

PythonProject5/                <-- Корень проекта
│
├── .venv/                     <-- Виртуальное окружение Python
│
├── data/                      <-- Папка с данными
│   └── products.json          <-- JSON с категориями и товарами
│
├── src/                       <-- Исходный код проекта
│   ├── __init__.py            <-- Инициализация модуля src
│   ├── models.py              <-- Классы Product и Category
│   ├── utils.py               <-- Функция load_data_from_json
│   └── main.py                <-- Точка входа, проверка загрузки данных
│
├── test/                      <-- Тесты для проекта
│   ├── __init__.py
│   ├── test_utils.py          <-- Тесты для utils.py
│   └── test_models.py         <-- Тесты для классов Product/Category
│
├── pyproject.toml             <-- Конфигурация проекта (для pytest, pip и др.)
└── README.md                  <-- Документация проекта


В модуле models созданы классы  Product  и Category.

Для класса Product определите следующие свойства:
название (name),описание (description),цена (
price),количество в наличии (quantity).
Для класса Category определите следующие свойства:
название (name),описание (description),список товаров категории (
products).

Также для класса Category добавлены атрибуты класса.
Доступ к этим атрибутам есть у каждого объекта класса, 
и в них храниться общая информация для всех объектов. 
Эти атрибуты хранят в себе количество категорий и количество товаров.

Атрибуты класса заполняются автоматически при инициализации нового объекта.

Написаны тесты для классов, которые проверяют корректность инициализации объектов класса 
Category, корректность инициализации объектов класса 
Product, подсчет количества продуктов,
подсчет количества категорий.

Реализуйте подгрузка данных по категориям и товарам из файла JSON. Для этого описана функция, которая будет читать 
файл и создавать объекты классов.  

Для класса Category  создан список товаров приватным атрибутом, чтобы к нему нельзя было получить доступ извне. Для добавления товаров в категорию реализуйте специальный метод 
add_product()  в классе Category, в который  передается объект класса 
Product  и уже его записывают в приватный атрибут списка товаров.


Для класса Product создан класс-метод new_product
, который принимает на вход параметры товара в словаре и возвращать созданный объект класса 
Product.

Для класса Product создан атрибут цены приватным и описаны геттеры и сеттеры. В сеттере реализована проверка: в случае если цена равна или ниже нуля, выводится сообщение в консоль 
“Цена не должна быть нулевая или отрицательная”
, при этом новая цена не устанавливается.

 Выделены две категории товаров и созданы под них классы-наследники:
«Смартфон» (Smartphone) и «Трава газонная» (LawnGrass), добавлены новые свойства.

Написаны тесты для новых функциональностей.

 Абстрактные классы и миксины

В проекте реализован абстрактный класс `BaseProduct`, который содержит
общую функциональность для всех товаров.

Также реализован миксин `InitMixin`, который выводит информацию
о создании объекта продукта.

Цепочка наследования:

BaseProduct → Product → Smartphone / LawnGrass
